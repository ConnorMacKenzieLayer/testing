#Use an Ubuntu 18.04 base for our staging server
FROM vm/ubuntu:18.04

COPY . .

RUN sudo apt-get update
RUN sudo apt-get install parallel

RUN chmod +x test.sh tests/one.sh tests/two.sh

RUN ./test.sh
